- [2026-03-04 14:59:03Z] Treat  as ABI source-of-truth independent of transient producer implementation details; schema now declares explicit binary offsets/sizes/reserved bytes and version dispatch behavior for major 1 and 2.
- [2026-03-04 14:59:03Z] Freeze v2 metadata header as 64 bytes and each plane descriptor as 24 bytes with fixed capacity=4 in a 256-byte metadata region, leaving payload start permanently at byte 256 ().
- [2026-03-04 14:59:03Z] Enforce deterministic active-plane semantics (contiguous slots from 0, packed payload offsets, inactive descriptors zeroed) to eliminate parser ambiguity across cv-mmap-gui and cvmmap-python-client during dual-parse rollout.
- [2026-03-04 14:59:21Z] Treat docs/cvmmap.ksy as ABI source-of-truth independent of transient producer implementation details; schema now declares explicit binary offsets/sizes/reserved bytes and version dispatch behavior for major 1 and 2.\n- [2026-03-04 14:59:21Z] Freeze v2 metadata header as 64 bytes and each plane descriptor as 24 bytes with fixed capacity=4 in a 256-byte metadata region, leaving payload start permanently at byte 256 (SHM_PAYLOAD_OFFSET).\n- [2026-03-04 14:59:21Z] Enforce deterministic active-plane semantics (contiguous slots from 0, packed payload offsets, inactive descriptors zeroed) to eliminate parser ambiguity across cv-mmap-gui and cvmmap-python-client during dual-parse rollout.\n- [2026-03-04 15:08:24Z] Kept transport message sections (`sync_message`, `module_status_message`, `control_message_request/response`) stable in structure while correcting control magic enum values to match protocol constants (0x3C/0x3D).
- [2026-03-04 15:08:24Z] Defined `frame_metadata` as a version-dispatched envelope (major=1 or 2) so both clients can implement dual-parse from one authoritative schema contract.
- [2026-03-04 15:08:24Z] Adopted explicit reserved-byte fields and fixed offsets in schema docs as normative ABI, avoiding compiler-native padding assumptions.
